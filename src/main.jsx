import React from "react";
import ReactDOM from "react-dom/client";
import StructCalcAI from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <StructCalcAI />
  </React.StrictMode>
);
